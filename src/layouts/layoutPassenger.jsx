export function PassengerLayount({ children }) {
    return (
        <div>
            <header>Passageiro Dashboard</header>
            <main>{children}</main>
            <footer>Passageiro Footer</footer>
        </div>
    );
};
